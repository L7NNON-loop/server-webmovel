{
  "name": "server-webmovel",
  "version": "1.0.0",
  "description": "Telegram automation backend ready for Render/Railway/Cloudflare containers",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/telegramService.js && node --check src/sessionStore.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "input": "^1.0.1",
    "telegram": "^2.26.22"
  }
}
